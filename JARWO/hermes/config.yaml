# Template config.yaml untuk Hermes Agent (NousResearch/hermes-agent).
# Isi asli sebaiknya diambil dari `hermes config get` di mesin Anda,
# bukan disalin mentah-mentah dari sini — dua field di bawah cuma
# starting point, bukan skema lengkap yang terverifikasi.
#
# Dokumentasi lengkap:
# https://hermes-agent.nousresearch.com/docs/user-guide/configuration

provider: ""   # contoh: nous / openrouter / openai / anthropic — lihat `hermes model`
model: ""      # contoh: "anthropic/claude-sonnet-4.5"

# Tambahkan pengaturan lain (gateway platform, dll) setelah dicek
# dengan `hermes config get` / `hermes doctor`.
